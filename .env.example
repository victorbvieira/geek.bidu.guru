# =============================================================================
# .env.example - geek.bidu.guru
# Copie este arquivo para .env e preencha com seus valores
#
# IMPORTANTE: PostgreSQL e remoto (VPS) - configure DATABASE_URL corretamente
# =============================================================================

# -----------------------------------------------------------------------------
# Ambiente
# -----------------------------------------------------------------------------
ENVIRONMENT=development  # development | staging | production
DEBUG=true               # true em dev, false em prod

# -----------------------------------------------------------------------------
# Banco de Dados (PostgreSQL 18 - REMOTO na VPS)
# -----------------------------------------------------------------------------
# OBRIGATORIO: Configure com as credenciais do seu PostgreSQL na VPS
#
# Desenvolvimento (database: geek_bidu_dev):
DATABASE_URL=postgresql+asyncpg://geek_app_dev:SUA_SENHA_DEV@SEU_IP_VPS:5432/geek_bidu_dev

# Testes (database: geek_bidu_test) - OPCIONAL, para rodar pytest --use-postgres
# Este banco sera TRUNCADO a cada execucao de testes!
DATABASE_TEST_URL=postgresql+asyncpg://geek_app_dev:SUA_SENHA_DEV@SEU_IP_VPS:5432/geek_bidu_test

# Producao (database: geek_bidu_prod):
# DATABASE_URL=postgresql+asyncpg://geek_app_prod:SUA_SENHA_PROD@SEU_IP_VPS:5432/geek_bidu_prod

# -----------------------------------------------------------------------------
# Redis (Cache) - Local no Docker Compose
# -----------------------------------------------------------------------------
# Porta 6380 local (evita conflito com outros projetos)
REDIS_URL=redis://redis:6379/0

# -----------------------------------------------------------------------------
# Seguranca
# -----------------------------------------------------------------------------
# IMPORTANTE: Gere uma chave segura para producao!
# python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=dev-secret-key-change-in-production-INSECURE

# JWT (sessao do admin)
# Tempo de expiracao do token em minutos (120 = 2 horas, 1440 = 24 horas)
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=120
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# -----------------------------------------------------------------------------
# Aplicacao
# -----------------------------------------------------------------------------
APP_NAME=geek.bidu.guru
# Desenvolvimento: porta 8001 (evita conflito com 8000)
APP_URL=http://localhost:8001
ALLOWED_HOSTS=localhost,127.0.0.1

# Producao:
# APP_URL=https://geek.bidu.guru
# ALLOWED_HOSTS=geek.bidu.guru,www.geek.bidu.guru

# -----------------------------------------------------------------------------
# APIs de Afiliados
# -----------------------------------------------------------------------------
# Amazon Associates
AMAZON_ACCESS_KEY=
AMAZON_SECRET_KEY=
AMAZON_PARTNER_TAG=
AMAZON_REGION=BR

# Mercado Livre
MELI_CLIENT_ID=
MELI_CLIENT_SECRET=
MELI_REDIRECT_URI=

# Shopee
SHOPEE_APP_ID=
SHOPEE_SECRET_KEY=

# -----------------------------------------------------------------------------
# IA / LLM
# -----------------------------------------------------------------------------
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# OpenRouter (acesso a multiplos modelos como Gemini, Claude, etc.)
# Crie uma conta em https://openrouter.ai e gere uma API key
OPENROUTER_API_KEY=

# -----------------------------------------------------------------------------
# Google Analytics 4 & Search Console
# -----------------------------------------------------------------------------
GA4_MEASUREMENT_ID=                   # Ex: G-XXXXXXXXXX
GA4_API_SECRET=
GOOGLE_SITE_VERIFICATION=             # Meta tag para verificacao GSC

# -----------------------------------------------------------------------------
# Email (Newsletter)
# -----------------------------------------------------------------------------
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=contato@geek.bidu.guru
SMTP_FROM_NAME=geek.bidu.guru

# -----------------------------------------------------------------------------
# n8n (Webhooks) - Container compartilhado na VPS
# -----------------------------------------------------------------------------
N8N_WEBHOOK_URL=
N8N_API_KEY=

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO  # DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_FORMAT=json # json | text
