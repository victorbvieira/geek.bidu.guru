# =============================================================================
# .env.docker - geek.bidu.guru (Docker Compose)
# =============================================================================
#
# Arquivo de variáveis de ambiente para Docker Compose no Dokploy.
# Este arquivo existe apenas para evitar erro:
# "env file /etc/dokploy/compose/.../code/.env not found"
#
# IMPORTANTE:
# - NÃO adicione valores reais/sensíveis aqui
# - Configure todas as variáveis de produção no painel do Dokploy
# - Use .env.production.example como referência
#
# O Dokploy injeta automaticamente as variáveis configuradas no painel,
# sobrescrevendo qualquer valor definido aqui.
#
# =============================================================================

# Valores padrão (serão sobrescritos pelo Dokploy)
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/dbname
SECRET_KEY=change-me
APP_URL=http://localhost:8000
ALLOWED_HOSTS=localhost
